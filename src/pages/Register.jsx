import RegisterForm from "../components/register/RegisterForm";

const Register = () => {
  return (
    <main className="dashboard-height w-screen flex justify-center items-center">
      <RegisterForm />
    </main>
  );
};

export default Register;
